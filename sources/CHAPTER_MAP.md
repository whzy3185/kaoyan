# 操作系统章节知识地图

更新时间：2026-09-20

## 第1章 操作系统基础

### 1-1 操作系统的基本概念
OS 定义；用户接口；资源管理；资源抽象；处理机、存储器、I/O、信息/文件管理。

### 1-2 操作系统的发展历程
未配置 OS；单道批处理；多道批处理；分时；实时；网络操作系统；分布式操作系统。

### 1-3 OS 的运行环境
OS 引导；OS 内核；CPU 双重工作模式；特权/非特权指令；系统调用；中断与异常。

### 1-4 操作系统结构
模块化；分层式；微内核；外核；内聚性与耦合度。

### 1-5 虚拟机
虚拟化；VMM；1型/2型虚拟机管理程序；主机/客户机；宿主 OS/客户 OS。

### 1-6 过程调用 vs 系统调用
caller/callee；参数；返回地址；现场保存与恢复；用户栈与栈帧；过程调用与系统调用执行环境差异。

## 第2章 进程管理

### 2-1 进程
定义与特征；进程状态与转换；进程管理数据结构；进程内存映像；进程控制；进程通信。

### 2-2 线程
多线程与地址空间；线程状态；TCB；线程控制；线程实现方式。

### 2-3 处理机调度
高级调度（作业/长程）；中级调度（内存/对换）；低级调度（进程/短程）；调度目标；调度算法。

### 2-4 同步与互斥
同步关系；互斥关系；临界资源；临界区；同步机制规则；软件同步；硬件同步；信号量。

### 2-5 经典同步问题与管程
管程与条件变量；生产者-消费者；读者-写者；哲学家进餐。

### 2-6 死锁
死锁定义；饥饿与死锁区分；产生原因；四个必要条件；处理方法。

## 第3章 内存管理

### 3-2 内存的分配
连续分配与离散分配；内存保护与共享；单一连续分配；固定分区；动态分区；内部/外部碎片；动态分区数据结构与分配算法；分页、分段、段页式等离散分配内容。

### 3-3 虚拟存储器
局部性原理；部分装入、请求调入与置换；请求分页；页面置换算法；虚拟存储器的多次性、对换性、虚拟性；抖动与工作集；内存映射文件。

> 当前尚未收到 3-1，待后续资料补齐后扩展本章开头部分。

## 第4章 文件系统

### 4-1 文件系统基础
数据项、记录、文件；文件名与类型；文件操作；访问控制；文件逻辑结构；文件物理结构；文件目录。

### 4-2 文件系统
文件系统层次结构；逻辑文件系统；文件组织模块；基本文件系统；I/O 控制；虚拟文件系统；外存管理；磁盘/分区/超级块/空闲空间/i 节点/根目录等文件系统布局。

## 第5章 I/O 与磁盘管理

### 5-1 I/O 系统
I/O 系统基本功能；设备无关性；处理机与设备并行；设备共享；错误处理；I/O 软件层次结构；用户层软件；设备独立性软件；设备驱动程序；中断处理程序；SPOOLing。

### 5-2 磁盘调度算法
磁盘初始化；低级/高级格式化；坏块；减少旋转延迟的方法；SSD 磨损均衡；FCFS；SSTF；SCAN；C-SCAN。

## 后续

本地图只覆盖当前已上传资料。新资料到达后继续按原文件编号扩展。
