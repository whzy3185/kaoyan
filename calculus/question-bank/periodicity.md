# 函数性质考察｜周期｜题面归档

来源：`1.函数极限连续.pdf`，PDF 第 12–14 页（题库正文第 6–8 页）。

> 本文件只保存题面，不保存答案。

## 1. 姜晓千真题同源 150（重点）

设 f(x) 为以 T 为周期的连续函数，则下列结论正确的个数是【 】

①

$$\int_0^x f(t)\,dt$$

以 T 为周期。

②

$$\int_0^x f(t)\,dt-\frac{x}{T}\int_0^T f(t)\,dt$$

以 T 为周期。

③ 若 f(x) 为奇函数，则

$$\int_0^x f(t)\,dt$$

以 T 为周期。

④

$$\int_0^x[f(t)-f(-t)]\,dt$$

以 T 为周期。

⑤ 若

$$\int_0^{+\infty}f(x)\,dx$$

收敛，则

$$\int_0^x f(t)\,dt$$

以 T 为周期。

A. 1  
B. 2  
C. 3  
D. 4

## 2. 25 版 1000 第八章强化 2

设 f(x) 是以 T 为周期的连续函数（若下式中用到 f'(x)，则设 f'(x) 存在），则以下结论中不正确的是（ ）。

A.

$$f'(x)$$

必以 T 为周期。

B.

$$\int_0^x f(t)\,dt$$

必以 T 为周期。

C.

$$\int_0^x[f(t)-f(-t)]\,dt$$

必以 T 为周期。

D.

$$\int_0^x f(t)\,dt-\frac{x}{T}\int_0^T f(t)\,dt$$

必以 T 为周期。

## 3. 25 版 660 数一二三第 195 题

下列叙述错误的是（ ）。

A. 设 f(x) 在 [-a,a] 上连续为奇函数，则 f(x) 在 [-a,a] 上的全体原函数为偶函数。

B. 设 f(x) 在 [-a,a] 上连续为偶函数，则 f(x) 在 [-a,a] 上的全体原函数为奇函数。

C. 设 f(x) 在 (-∞,+∞) 内连续，以 T 为周期且为奇函数，则

$$\int_0^x f(t)\,dt$$

也是以 T 为周期的函数。

D. 设 f(x) 在 (-∞,+∞) 内连续，以 T 为周期，又

$$\int_0^{+\infty}f(x)\,dx$$

收敛，则

$$\int_0^x f(t)\,dt$$

也是以 T 为周期的函数。

## 4. 25 版 660 数一二三第 196 题

设 f(x) 为以 T 为周期的非零连续函数，

$$\Phi(x)=\int_a^x[f(t)-f(-t)]\,dt$$

a 是常数，则（ ）。

A. Φ(x) 是以 T 为周期的偶函数。  
B. Φ(x) 是以 T 为周期的奇函数。  
C. Φ(x) 是偶函数，但不一定以 T 为周期。  
D. Φ(x) 是奇函数，但不一定以 T 为周期。

## 5. 24 版 1000 第八章 C 组 1

设 f(x) 在 (-∞,+∞) 内连续，下述命题中：

（1）对任意 a，

$$\int_{-a}^{a}f(x)\,dx=0$$

的充要条件是 f(x) 为奇函数；

（2）对任意 a，

$$\int_{-a}^{a}f(x)\,dx=2\int_0^a f(x)\,dx$$

的充要条件是 f(x) 为偶函数；

（3）对任意 a，

$$\int_a^x f(t)\,dt$$

具有周期 T 的充要条件是 f(x) 具有周期 T。

正确的个数为（ ）。

A. 0  
B. 1  
C. 2  
D. 3

## 6. 900 第三章数二 B 类 15

设定义在 (-∞,+∞) 上的连续函数 f(x) 的图形关于 x=0 与 x=1 均对称，则下列命题中，正确命题为（ ）。

① 若

$$\int_0^1 f(x)\,dx=0$$

则

$$\int_0^x f(t)\,dt$$

为周期函数。

② 若

$$\int_0^2 f(x)\,dx=0$$

则

$$\int_0^x f(t)\,dt$$

为周期函数。

③

$$\int_0^x f(t)\,dt-x\int_0^2 f(t)\,dt$$

为周期函数。

④

$$\int_0^x f(t)\,dt-\frac{x}{2}\int_0^2 f(t)\,dt$$

为周期函数。

A. ②③  
B. ②④  
C. ①②③  
D. ①②④

## 7. 24 版 1000 第八章 B 组 2

设 f(x) 是以 2 为周期的连续函数，

$$G(x)=2\int_0^x f(t)\,dt-x\int_0^2 f(t)\,dt$$

则（ ）。

A. G(x) 是以 2 为周期的周期函数，G'(x) 也是以 2 为周期的周期函数。  
B. G(x) 是以 2 为周期的周期函数，G'(x) 不是以 2 为周期的周期函数。  
C. G(x) 不是以 2 为周期的周期函数，G'(x) 是以 2 为周期的周期函数。  
D. G(x) 不是以 2 为周期的周期函数，G'(x) 也不是以 2 为周期的周期函数。

## 8. 25 版 1000 第九章基础 14

若连续周期函数 y=f(x)（不恒为常数）对任何 x，恒有

$$\int_{-1}^{x+6}f(t)\,dt+\int_{x-3}^{4}f(t)\,dt=14$$

成立，则 f(x) 的周期是（ ）。

A. 7  
B. 8  
C. 9  
D. 10

## 9. 26 版 660 数三第 616 题

设函数 f(x) 满足

$$f\left(x+\frac12\right)=\frac12+\sqrt{f(x)-f^2(x)},\quad x\in(-\infty,+\infty)$$

则 f(x) 的周期为（ ）。

A. 1  
B.

$$\frac12$$

C.

$$\frac13$$

D.

$$\frac14$$

## 10. 26 版 1000 第八章强化 2

设 f(x) 是以 T 为周期的连续奇函数，则

$$\int_{a+(b+1)T}^{a+(b+2)T}f'(x)\,dx$$

的取值（ ）。

A. 与 a,b,T 均有关。  
B. 与 a,b,T 均无关。  
C. 只与 T 有关。  
D. 只与 a,T 有关。
