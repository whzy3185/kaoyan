# 2019 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 45、46 题**

## 题目定位

- **12（选择，计算机系统概述 / 冯·诺依曼结构）**：下列关于冯·诺依曼结构计算机基本思想的叙述中，错误的是（ ）。
- **13（选择，数据表示与运算 / 类型转换与符号扩展）**：考虑以下 C 语言代码： （含给定代码） 执行上述程序段后，si 的值是（ ）。
- **14（选择，存储系统 / 虚拟存储、页表与TLB）**：下列关于缺页处理的叙述中，错误的是（ ）。
- **15（选择，指令系统 / 寻址方式）**：某计算机采用大端方式，按字节编址。某指令中操作数的机器数为 1234FF00H，该操作数采用基址寻址方式，形式地址（用补码表示）为 FF12H，基址寄存器内容为 F0000000H，则该操作数的 LSB（最低有效字节）所在的地址是（ ）。
- **16（选择，计算机系统概述 / 性能指标与CPU时间）**：下列有关处理器时钟脉冲信号的叙述中，错误的是（ ）。
- **17（选择，中央处理器 / 数据通路与指令执行）**：某指令功能为 `R[r2] ← R[r1] + M[R[r0]]`，其两个源操作数分别采用寄存器、寄存器间接寻址方式。对于下列给定部件，该指令在取数及执行过程中需要用到的是（ ）。 Ⅰ. 通用寄存器组（GPRs） Ⅱ. 算术逻辑单元（ALU） Ⅲ. 存储器（Memory…
- **18（选择，中央处理器 / 指令流水线）**：在采用"取指、译码/取数、执行、访存、写回"5 段流水线的处理器中，执行如下指令序列： （含给定代码） 下列指令对中，不存在数据冒险的是（ ）。
- **19（选择，总线 / 总线性能与带宽）**：假定一台计算机采用 3 通道存储器总线，配套的内存条型号为 DDR3-1333，即内存条所接插的存储器总线的工作频率为 1333MHz，总线宽度为 64 位，则存储器总线的总带宽大约是（ ）。
- **20（选择，输入输出系统 / DMA）**：下列关于磁盘存储器的叙述中，错误的是（ ）。
- **21（选择，中央处理器 / CPU综合）**：某设备以中断方式与 CPU 进行数据交换，CPU 主频为 1GHz，设备接口中的数据缓冲寄存器为 32 位，设备的数据传输率为 50KBps。若每次中断开销（包括中断响应和中断处理）为 1000 个时钟周期，则 CPU 用于该设备输入/输出的时间占整个 CPU 时间的百…
- **22（选择，输入输出系统 / DMA）**：下列关于 DMA 方式的叙述中，正确的是（ ）。 I. DMA 传送前由设备驱动程序设置传送参数 II. 数据传送前由 DMA 控制器请求总线使用权 III. 数据传送由 DMA 控制器直接控制总线完成 IV. DMA 传送结束后的处理由中断服务程序完成
- **45（综合，数据表示与运算 / 整数编码与补码）**：已知 $f(n) = n! = n \times (n-1) \times (n-2) \times \cdots \times 2 \times 1$，计算 $f(n)$ 的 C 语言函数 f1 的源程序（阴影部分）及其在 32 位计算机 M 上的部分机器级代码如下： （含给定代码） 其中，机器级代码行包括行号、虚拟地址、机器指令和汇编指令，计算机 M 按字节编址，int 型数据占 32 位。请回答下列问题： (1…
- **46（综合，存储系统 / 虚拟存储、页表与TLB）**：> 【承上题·2019 年第 45 题材料】 本题与第 45 题为同一组连续大题，共用下面这段代码。为便于独立作答，材料一并附在此处。 已知 $f(n) = n! = n \times (n-1) \times (n-2) \times \cdots \times 2 \times 1$，计算 $f(n)$ 的 C 语言函数 f1 的源程序（阴影部分）及其在 32 位计算机 M 上的部分机器级代码如下： （含给定代码…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
