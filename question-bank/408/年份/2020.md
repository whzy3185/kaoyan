# 2020 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 字长与编址）**：下列给出的部件中其位数（宽度）一定与机器字长相同的是（ ）。 I、ALU II、指令寄存器 III、通用寄存器 IV、浮点寄存器
- **13（选择，数据表示与运算 / 浮点数与IEEE 754）**：已知带符号整数用补码表示，float 型数据用 IEEE 754 标准表示，假定变量 x 的类型只能是 int 或 float。当 x 的机器数为 C800 0000H 时，x 的值可能是（ ）。
- **14（选择，数据表示与运算 / 数据对齐）**：在按字节编址，采用小端方式的 32 位计算机中，按边界对齐方式为以下 C 语言结构型变量 a 分配存储空间。 （含给定代码） 若 `a` 的首地址为 2020FE00H，a 的成员变量 `x2` 的机器数为 12340000H，则其中 34H 所在存储单元的地址是（ ）…
- **15（选择，存储系统 / 虚拟存储、页表与TLB）**：下列关于 TLB 和 Cache 的叙述中，错误的是（ ）。
- **16（选择，指令系统 / 指令格式与编码）**：某计算机采用 16 位定长指令字格式，操作码位数和寻址方式位数固定，指令系统有 48 条指令，支持直接、间接、立即、相对 4 种寻址方式，单地址指令中直接寻址方式可寻址范围是（ ）。
- **17（选择，中央处理器 / 指令流水线）**：下列给出的处理器类型中理想情况下 CPI 为 1 的是（ ）。 I、单周期 CPU； II、多周期 CPU； III、基本流水线 CPU； IV、超标量流水线 CPU
- **18（选择，中央处理器 / 异常与中断）**：下列关于"自陷"（Trap，也称陷阱）的叙述中错误的是（ ）。
- **19（选择，总线 / 总线性能与带宽）**：QPI 总线是一种点对点全工双同步串行总线，总线上的设备可同时接收和发送信息，每个方向可同时传输 20 位信息（16 位数据 + 4 位校验位），每个 QPI 数据包有 80 位信息，分 2 个时钟周期传送，每个时钟周期传递 2 次，因此 QPI 总线带宽为每秒传送次数…
- **20（选择，中央处理器 / 异常与中断）**：下列事件中属于外部中断事件的是（ ）。 I、访存时缺页； II、定时器到时； III、网络数据包到达；
- **21（选择，中央处理器 / 异常与中断）**：外部中断包括不可屏蔽中断（NMI）和可屏蔽中断，下列关于外部中断的叙述中错误的是（ ）。
- **22（选择，输入输出系统 / DMA）**：若设备采用周期挪用 DMA 方式进行输入输出，每次 DMA 传送的数据块大小为 512 字节，相应的 I/O 接口中有一个 32 位数据缓冲寄存器，对于数据输入过程，下列叙述中错误的是（ ）。
- **43（综合，数据表示与运算 / 定点运算、标志位与溢出）**：有实现 $x \times y$ 的两个 C 语言函数如下： （含给定代码） 假定某计算机 M 中 ALU 只能进行加减运算和逻辑运算。请回答下列问题。 (1) 若 M 的指令系统中没有乘法指令，但有加法、减法和移位等指令，则在 M 上也能实现上述两个函数中的乘法运算，为什么？ (2) 若 M 的指令系统中有乘法指令，则基于 ALU、移位器、寄存器以及相应控制逻辑实现乘法指令时，控制逻辑的作用是什么？ (3) 针对以…
- **44（综合，存储系统 / Cache）**：假定主存地址为 32 位，按字节编址，指令 Cache 和数据 Cache 与主存之间均采用 8 路组相联映射方式，直写（WriteThrough）写策略和 LRU 替换算法，主存块大小为 64B，数据区容量各为 32KB。开始时 Cache 均为空。请回答下列问题。 (1) Cache 每一行中标记（Tag）、LRU 位各占几位？是否有修改位？ (2) 有如下 C 语言程序段： （含给定代码） 若数组 s 及其变量…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
