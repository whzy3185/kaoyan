# 2011 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 性能指标与CPU时间）**：下列选项中，描述浮点数操作速度指标的是（ ）。
- **13（选择，数据表示与运算 / 浮点数与IEEE 754）**：float 型数据通常用 IEEE754 单精度浮点数格式表示。若编译器将 float 型变量 x 分配在一个 32 位浮点寄存器 FR1 中，且 x=-8.25，则 FR1 的内容是( )
- **14（选择，存储系统 / 半导体存储器与DRAM）**：下列各类存储器中，不采用随机存取方式的是（ ）。
- **15（选择，存储系统 / 存储系统综合）**：某计算机存储器按字节编址，主存地址空间大小为 64MB，现用 4M×8 位的 RAM 芯片组成 32MB 的主存储器，则存储器地址寄存器 MAR 的位数至少是（ ）。
- **16（选择，指令系统 / 寻址方式）**：偏移寻址将某个寄存器内容与一个形式地址相加而生成有效地址。下列寻址方式中，不属于偏移寻址方式的是（ ）。
- **17（选择，数据表示与运算 / 定点运算、标志位与溢出）**：某机器有一个标志寄存器，其中有进位/借位标志 CF、零标志 ZF、符号标志 SF 和溢出标志 OF，条件转移指令 bgt（无符号整数比较大于时转移）的转移条件是 ( )
- **18（选择，中央处理器 / 指令流水线）**：下列给出的指令系统特点中，有利于实现指令流水线的是（ ）。 Ⅰ. 指令格式规整且长度一致 Ⅱ. 指令和数据按边界对齐存放 Ⅲ. 只有 Load/Store 指令才能对操作数进行存储访问
- **19（选择，中央处理器 / 控制器与指令周期）**：假定不采用 Cache 和指令预取技术，且机器处于"开中断"状态，则在下列有关指令执行的叙述中，错误的是（ ）。
- **20（选择，总线 / 总线组成与信号）**：在系统总线的数据线上，不可能传输的是（ ）。
- **21（选择，中央处理器 / 异常与中断）**：某计算机有五级中断 $L_4 \sim L_0$，中断屏蔽字为 $M_4 M_3 M_2 M_1 M_0$，$M_i = 1$（$0 \le i \le 4$）表示对 $L_i$ 级中断进行屏蔽。若中断响应优先级从高到低的顺序是 $L_0 \to L_1 \to L_2…
- **22（选择，输入输出系统 / DMA）**：某计算机处理器主频为 50 MHz，采用定时查询方式控制设备 A 的 I/O，查询程序运行一次所用的时钟周期数至少为 500。在设备 A 工作期间，为数据不丢失，每秒需对其查询至少 200 次，则 CPU 用于设备 A 的 I/O 的时间占整个 CPU 时间的百分比至少…
- **43（综合，数据表示与运算 / 定点运算、标志位与溢出）**：假定在一个 8 位字长的计算机中运行如下 C 程序段： （含给定代码） 若编译器编译时将 8 个 8 位寄存器 R1～R8 分别分配给变量 x、y、m、n、z1、z2、k1 和 k2。提示：带符号整数用补码表示。 （1）执行上述程序段后，寄存器 R1、R5 和 R6 的内容分别是什么（用十六进制表示）？ （2）执行上述程序段后，变量 m 和 k1 的值分别是多少（用十进制表示）？ （3）上述程序段涉及带符号整数加/减…
- **44（综合，存储系统 / 虚拟存储、页表与TLB）**：某计算机存储器按字节编址，虚拟（逻辑）地址空间大小为 16 MB，主存（物理）地址空间大小为 1 MB，页面大小为 4 KB；Cache 采用直接映射方式，共 8 行；主存与 Cache 之间交换的块大小为 32 B。系统运行到某一时刻时，页表的部分内容和 Cache 的部分内容如下，图中页框号及标记字段的内容均为十六进制形式。 （1）虚拟地址共有几位，哪几位表示虚页号？物理地址共有几位，哪几位表示页框号（物理页号）…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
