# 2017 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / ISA与软硬件接口）**：假定计算机 M1 和 M2 具有相同的指令集体系结构（ISA），主频分别为 1.5GHz 和 1.2GHz。在 M1 和 M2 上运行某基准程序 P，平均 CPI 分别为 2 和 1，则程序 P 在 M1 和 M2 上运行时间的比值是（ ）。
- **13（选择，存储系统 / 存储层次、局部性与性能）**：某计算机主存按字节编址，由 4 个 64M×8 位的 DRAM 芯片采用交叉编址方式构成，并与宽度为 32 位的存储器总线相连，主存每次最多读写 32 位数据。若 double 型变量 x 的主存地址为 804001AH，则读取 x 需要的存储周期数是（ ）。
- **14（选择，存储系统 / Cache）**：某 C 语言程序段如下： （含给定代码） 下列关于数组 a 的访问局部性的描述中，正确的是（ ）。
- **15（选择，指令系统 / 寻址方式）**：下列寻址方式中，最适合按下标顺序访问一维数组元素的是（ ）。
- **16（选择，指令系统 / 扩展操作码）**：某计算机按字节编址，指令字长固定且只有两种指令格式，其中三地址指令 29 条，二地址指令 107 条，每个地址字段为 6 位，则指令字长至少应该是（ ）。
- **17（选择，中央处理器 / 指令流水线）**：下列关于超标量流水线特性的叙述中，正确的是（ ）。 Ⅰ. 能缩短流水线功能段的处理时间 Ⅱ. 能在一个时钟周期内同时发射多条指令 Ⅲ. 能结合动态调度技术提高指令执行并行性
- **18（选择，中央处理器 / 硬布线与微程序控制）**：下列关于主存储器（MM）和控制存储器（CS）的叙述中，错误的是（ ）。
- **19（选择，中央处理器 / 数据通路与指令执行）**：下列关于指令流水线数据通路的叙述中，错误的是（ ）。
- **20（选择，总线 / 总线仲裁）**：下列关于多总线结构的叙述中，错误的是（ ）。
- **21（选择，输入输出系统 / DMA）**：I/O 指令实现的数据传送通常发生在（ ）。
- **22（选择，输入输出系统 / DMA）**：下列关于多重中断系统的叙述中，错误的是（ ）。
- **43（综合，数据表示与运算 / 浮点数与IEEE 754）**：已知 $f(n) = \sum_{i=0}^{n} 2^i = 2^{n+1} - 1 = \underbrace{11\cdots1}_{n+1 \text{ 个 } 1}\text{B}$，计算 $f(n)$ 的 C 语言函数 f1 如下： （含给定代码） 将 f1 中的 int 都改为 float，可得到计算 f(n) 的另一个函数 f2。假设 unsigned 和 int 型数据都占 32 位，float 采…
- **44（综合，指令系统 / CISC与RISC）**：在按字节编址的计算机 M 上，题 43 中 f1 的部分源程序（阴影部分）与对应的机器级代码（包括指令的虚拟地址）如下图所示。 （含给定代码） 其中，机器级代码行包括行号、虚拟地址、机器指令和汇编指令。 请回答下列问题。 (1) 计算机 M 是 RISC 还是 CISC？为什么？ (2) f1 的机器指令代码共占多少字节？要求给出计算过程。 (3) 第 20 条指令 cmp 通过 i 减 n-1 实现对 i 和 n-…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
