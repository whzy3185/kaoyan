# 2010 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 性能指标与CPU时间）**：能缩短程序执行时间的措施是( )。 I. 提高 CPU 时钟频率 II. 优化数据通路结构 III. 对程序进行编译优化
- **13（选择，数据表示与运算 / 定点运算、标志位与溢出）**：假定有 4 个整数用 8 位补码分别表示 r1=FEH，r2=F2H，r3=90H，r4=F8H，若将运算结果存放在一个 8 位寄存器中，则下列运算中会发生溢出的是( )。
- **14（选择，数据表示与运算 / 浮点数与IEEE 754）**：假定变量 `i`、`f` 和 `d` 的数据类型分别为 `int`、`float` 和 `double`（`int` 用补码表示，`float` 和 `double` 分别用 IEEE 754 单精度和双精度浮点数格式表示），已知 `i=785`，`f=1.5678e3…
- **15（选择，存储系统 / 主存芯片扩展与编址）**：假定用若干个 2K×4 位的芯片组成一个 8K×8 位的存储器，则地址 0B1FH 所在芯片的最小地址是( )。
- **16（选择，存储系统 / 半导体存储器与DRAM）**：下列有关 RAM 和 ROM 的叙述中，正确的是( )。 I. RAM 是易失性存储器，ROM 是非易失性存储器 II. RAM 和 ROM 都采用随机存取方式进行信息访问 III. RAM 和 ROM 都可用作 Cache IV. RAM 和 ROM 都需要进行刷新
- **17（选择，存储系统 / 虚拟存储、页表与TLB）**：下列命中组合情况中，一次访存过程中不可能发生的是( )。
- **18（选择，中央处理器 / 控制器与指令周期）**：下列寄存器中，汇编语言程序员可见的是( )。
- **19（选择，中央处理器 / 指令流水线）**：下列选项中，不会引起指令流水线阻塞的是( )。
- **20（选择，总线 / 总线基本概念）**：下列选项中的英文缩写均为总线标准的是( )。
- **21（选择，中央处理器 / 异常与中断）**：单级中断系统中，中断服务程序内的执行顺序是( )。 I. 保护现场 II. 开中断 III. 关中断 IV. 保存断点 V. 中断事件处理 VI. 恢复现场 VII. 中断返回
- **22（选择，存储系统 / 存储层次、局部性与性能）**：假定一台计算机的显示存储器用 DRAM 芯片实现，若要求显示分辨率为 $1600\times1200$，颜色深度为 24 位，帧频为 85Hz，显存总带宽的 50% 用来刷新屏幕，则需要的显存总带宽至少约为（ ）。
- **43（综合，指令系统 / 扩展操作码）**：某计算机字长为 16 位，主存地址空间大小为 128 KB，按字编址。采用单字长指令格式，指令各字段定义如下： | 位号 | 15～12 | 11～9 | 8～6 | 5～3 | 2～0 | | --- | --- | --- | --- | --- | --- | | 字段 | OP | Ms | Rs | Md | Rd | | 分组 | 操作码 | 源操作数 | 源操作数 | 目的操作数 | 目的操作数 | 转…
- **44（综合，存储系统 / Cache）**：某计算机的主存地址空间大小为 256 MB，按字节编址。指令 Cache 和数据 Cache 分离，均有 8 个 Cache 行，每个 Cache 行大小为 64 B，数据 Cache 采用直接映射方式。现有两个功能相同的程序 A 和 B，其伪代码如下： （含给定代码） 假定 int 型数据用 32 位补码表示，程序编译时 i、j、sum 均分配在寄存器中，数组 a 按行优先方式存放，其首地址为 320（十进制数）…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
