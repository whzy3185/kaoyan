# 2013 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 性能指标与CPU时间）**：某计算机主频为 1.2 GHz，其指令分为 4 类，它们在基准程序中所占比例及 CPI 如下表所示。 | 指令类型 | 所占比例 | CPI | | --- | ---: | ---: | | A | 50% | 2 | | B | 20% | 3 | | C | 10…
- **13（选择，数据表示与运算 / 浮点数与IEEE 754）**：某数采用 IEEE754 单精度浮点数格式表示为 C6400000H，则该数的值是（ ）。
- **14（选择，数据表示与运算 / 整数编码与补码）**：某字长为 8 位的计算机中，已知整型变量 x、y 的机器数分别为 $[x]_补$ = 11110100， $[y]_补$ = 10110000。若整型变量 z = 2x + y/2，则 z 的机器数为（ ）。
- **15（选择，数据表示与运算 / 校验码）**：用海明码对长度为 8 位的数据进行检/纠错时，若能纠正一位错，则校验位数至少为（ ）。
- **16（选择，存储系统 / 虚拟存储、页表与TLB）**：某计算机主存地址空间大小为 256 MB，按字节编址。虚拟地址空间大小为 4 GB，采用页式存储管理，页面大小为 4 KB。TLB（快表）采用全相联映射，有 4 个页表项，内容如下表所示。 | 有效位 | 标记 | 页框号 | … | | --- | --- | ---…
- **17（选择，指令系统 / 寻址方式）**：假设变址寄存器 R 的内容为 1000H，指令中的形式地址为 2000H；地址 1000H 中的内容为 2000H，地址 2000H 中的内容为 3000H，地址 3000H 的内容为 4000H，则变址寻址方式下访问到的操作数是（）。
- **18（选择，中央处理器 / 指令流水线）**：某 CPU 主频为 1.03GHz，采用 4 级指令流水线，每个段的执行需要 1 个时钟周期。假定 CPU 执行了 100 条指令，在其执行过程中没有发生任何流水线阻塞，此时流水线的吞吐率为（ ）。
- **19（选择，总线 / 总线基本概念）**：下列选项中，用于设备和设备控制器（I/O 接口）之间互连的接口标准是（ ）。
- **20（选择，存储系统 / 外部存储器）**：下列选项中，用于提高 RAID 可靠性的措施有（ ）。 I. 磁盘镜像 Ⅱ. 条带化 Ⅲ. 奇偶校验 Ⅳ. 增加 Cache 机制
- **21（选择，输入输出系统 / DMA）**：某磁盘的转速为 10000rpm，平均寻道时间是 6ms，磁盘传输速率是 20MB/s，磁盘控制器延时为 0.2ms，读取一个 4KB 的扇区所需的平均时间约为（ ）。
- **22（选择，输入输出系统 / DMA）**：下列关于中断 I/O 方式和 DMA 方式比较的叙述中，错误的是（ ）。
- **43（综合，存储系统 / 存储系统综合）**：某 32 位计算机，CPU 主频为 800MHz，Cache 命中时的 CPI 为 4，Cache 块大小为 32 字节；主存采用 8 体交叉存储方式，每个体的存储字长为 32 位、存储周期是 40ns；存储器总线宽度为 32 位，总线时钟频率为 200MHz，支持突发传送总线事务。每次读突发传送总线事务的过程包括：送首地址和命令、存储器准备数据、传送数据。每次突发传送 32 字节，传送地址或者 32 位数据均需要一…
- **44（综合，指令系统 / 寻址方式）**：某计算机采用 16 位定长指令字格式，其 CPU 中有一个标志寄存器，其中包含进位/借位标志 CF、零标志 ZF 和符号标志 NF。假定为该机设计了条件转移指令，其格式如下： | 位号 | 15～11 | 10 | 9 | 8 | 7～0 | | --- | --- | --- | --- | --- | --- | | 字段 | 00000 | C | Z | N | OFFSET | 其中，00000 为操作码…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
