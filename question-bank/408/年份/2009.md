# 2009 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 11～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **11（选择，计算机系统概述 / 冯·诺依曼结构）**：冯·诺依曼计算机中指令和数据均以二进制存放，CPU 区分它们的依据是（ ）。
- **12（选择，数据表示与运算 / 类型转换与符号扩展）**：一个 C 语言程序在一台 32 位机器上运行。程序中定义了三个变量 x，y 和 z，其中 x 和 z 是 int 型，y 为 short 型。当 x=127，y=-9 时，执行赋值语句 z=x+y 后，x、y 和 z 的值分别是：
- **13（选择，数据表示与运算 / 浮点数与IEEE 754）**：浮点数加、减运算过程一般包括对阶、尾数运算、规格化、舍入和判溢出等步骤。设浮点数的阶码和尾数均采用补码表示，且位数分别为 5 位和 7 位（均含 2 位符号位）。若有两个数 X=$2^7 \times 29/32$，Y=$2^5 \times 5/8$，则用浮点加法计算…
- **14（选择，存储系统 / Cache）**：某计算机的 Cache 共有 16 块，采用 2 路组相联映射方式（即每组 2 块）。每个主存块大小为 32 字节，按字节编址。主存 129 号单元所在主存块应装入到的 Cache 组号是（ ）。
- **15（选择，存储系统 / 主存芯片扩展与编址）**：某计算机主存容量为 64KB，其中 ROM 区为 4KB，其余为 RAM 区，按字节编址。现要用 2K×8 位的 ROM 芯片和 4K×4 位的 RAM 芯片来设计该存储器，则需要上述规格的 ROM 芯片数和 RAM 芯片数分别是（）。
- **16（选择，指令系统 / 寻址方式）**：某机器字长 16 位，主存按字节编址，转移指令采用相对寻址，由两个字节组成，第一字节为操作码字段，第二字节为相对位移量字段。假定取指令时，每取一个字节 PC 自动加 1。若某转移指令所在主存地址为 2000H，相对位移量字段的内容为 06H，则该转移指令成功转移后的目标…
- **17（选择，指令系统 / CISC与RISC）**：关于 RISC 的叙述，错误的是
- **18（选择，中央处理器 / 指令流水线）**：某计算机的指令流水线由四个功能段组成，指令流经各功能段的时间（忽略各功能段之间的缓存时间）分别为 90 ns、80 ns、70 ns、和 60 ns，则该计算机的 CPU 时钟周期至少是（）。
- **19（选择，中央处理器 / 硬布线与微程序控制）**：相对于微程序控制器，硬布线控制器的特点是
- **20（选择，总线 / 总线性能与带宽）**：假设某系统总线在一个总线周期中并行传输 4 字节信息，一个总线周期占用 2 个时钟周期，总线时钟频率为 10MHz，则总线带宽是（）。
- **21（选择，存储系统 / Cache）**：假设某计算机的存储系统由 Cache 和主存组成，某程序执行过程中访存 1000 次，其中访问 Cache 缺失（未命中）50 次，则 Cache 的命中率是（）。
- **22（选择，中央处理器 / 异常与中断）**：下列选项中，能引起外部中断的事件是（ ）。
- **43（综合，输入输出系统 / DMA）**：某计算机的 CPU 主频为 500MHz，CPI 为 5（即执行每条指令平均需 5 个时钟周期）。假定某外设的数据传输率为 0.5MB/s，采用中断方式与主机进行数据传送，以 32 位为传输单位，对应的中断服务程序包含 18 条指令，中断服务的其他开销相当于 2 条指令的执行时间。请回答下列问题，要求给出计算过程。 (1) 在中断方式下，CPU 用于该外设 I/ O 的时间占整个 CPU 时间的百分比是多少？ (2)…
- **44（综合，中央处理器 / 数据通路与指令执行）**：某计算机字长为 16 位，采用 16 位定长指令字结构，部分数据通路结构如下图所示。图中所有控制信号为 1 时表示有效、为 0 时表示无效。例如，控制信号 MDRinE 为 1 表示允许数据从 DB 打入 MDR，MDRin 为 1 表示允许数据从内总线打入 MDR。假设 MAR 的输出一直处于使能状态。加法指令“ADD (R1), R0”的功能为 (R0) + ((R1)) → (R1)，即将 R0 中的数据与 R…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
