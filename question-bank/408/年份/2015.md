# 2015 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 程序执行、编译与链接）**：计算机硬件能够直接执行的语言 Ⅰ. 机器语言程序 Ⅱ. 汇编语言程序 Ⅲ. 硬件描述语言程序
- **13（选择，数据表示与运算 / 整数编码与补码）**：由 3 个"1"和 5 个"0"组成的 8 位二进制补码，能表示的最小整数（ ）。
- **14（选择，数据表示与运算 / 浮点数与IEEE 754）**：下列有关浮点数加减运算的叙述中，正确的是（ ）。 Ⅰ. 对阶操作不会引起阶码上溢或下溢 Ⅱ. 右规和尾数舍入都可能引起阶码上溢 Ⅲ. 左规时可能引起阶码下溢 Ⅳ. 尾数溢出时结果不一定溢出
- **15（选择，存储系统 / Cache）**：假定主存地址位数为 32 位，按字节编址，主存和 Cache 之间采用直接映射方式，主存块大小为 4 个字，每字 32 位，写操作时采用回写 (Write Back) 方式，则能存放 4K 字数据的 Cache 的总容量的位数至少是（ ）。
- **16（选择，存储系统 / 虚拟存储、页表与TLB）**：假定编译器将赋值语句 "x=x+3;" 转换为指令 "add xaddr, 3"，其中 xaddr 是 x 对应的存储单元地址。若执行该指令的计算机采用页式虚拟存储管理方式，并配有相应的 TLB，且 Cache 使用直写（Write Through）方式，则完成该指令功…
- **17（选择，存储系统 / 存储层次、局部性与性能）**：下列存储器中，在工作期间需要刷新的是（ ）。
- **18（选择，存储系统 / 存储层次、局部性与性能）**：某计算机使用 4 体交叉编址存储器，假定在存储器总线上出现的主存地址（十进制）序列为 8005，8006，8007，8008，8001，8002，8003，8004，8000，则可能发生访存冲突的地址对是（ ）。
- **19（选择，总线 / 总线基本概念）**：下列有关总线定时的叙述中，错误的是（ ）。
- **20（选择，输入输出系统 / DMA）**：若磁盘转速为 7200rpm，平均寻道时间为 8ms，每个磁道包含 1000 个扇区，则访问一个扇区的平均存取时间大约是（ ）。
- **21（选择，输入输出系统 / DMA）**：在采用中断 I/O 方式控制打印输出的情况下，CPU 和打印控制接口中的 I/O 端口之间交换的信息不可能是（ ）。
- **22（选择，中央处理器 / 异常与中断）**：内部异常（内中断）可分为故障（fault）、陷阱（trap）和终止（abort）三类。下列有关内部异常的叙述中，错误的是（ ）。
- **43（综合，中央处理器 / 数据通路与指令执行）**：某 16 位计算机的主存按字节编址，存取单位为 16 位；采用 16 位定长指令字格式；CPU 采用单总线结构，主要部分如下图所示。 图中 R0～R3 为通用寄存器；T 为暂存器；SR 为移位寄存器，可实现直送（mov）、左移一位（left）和右移一位（right）3 种操作，控制信号为 SRop，SR 的输出由信号 SRout 控制；ALU 可实现直送 A（mova）、A 加 B（add）、A 减 B（sub）、A…
- **44（综合，中央处理器 / 数据通路与指令执行）**：题 43 中描述的计算机如下图所示。为便于从本题入口独立作答，这里先附该 CPU 单总线数据通路图。 其部分指令执行过程的控制信号如题 44 图 a 所示 图 a 部分指令控制信号 该机指令格式如题 44 图 b 所示，支持寄存器直接和寄存器间接两种寻址方式，寻址方式位分别为 0 和 1，通用寄存器 R0～R3 的编号分别为 0、1、2 和 3。 请回答下列问题。 (1) 该机的指令系统最多可定义多少条指令？ (2)…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
