# 2014 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 44、45 题**

## 题目定位

- **12（选择，计算机系统概述 / 性能指标与CPU时间）**：程序 P 在机器 M 上的执行时间是 20 秒，编译优化后，P 执行的指令数减少到原来的 70%，而 CPI 增加到原来的 1.2 倍，则 P 在 M 上的执行时间是（ ）。
- **13（选择，数据表示与运算 / 定点运算、标志位与溢出）**：若 x=103，y=-25，则下列表达式采用 8 位定点补码运算实现时，会发生溢出的是（ ）。
- **14（选择，数据表示与运算 / 浮点数与IEEE 754）**：float 型数据常用 IEEE754 单精度浮点格式表示。假设两个 float 型变量 x 和 y 分别存放在 32 位寄存器 f1 和 f2 中，若 (f1)=CC900000H，(f2)=B0C00000H，则 x 和 y 之间的关系为（ ）。
- **15（选择，存储系统 / 半导体存储器与DRAM）**：某容量为 256MB 的存储器由若干 4M×8 位的 DRAM 芯片构成，该 DRAM 芯片的地址引脚和数据引脚总数是（ ）。
- **16（选择，存储系统 / 存储系统综合）**：采用指令 Cache 与数据 Cache 分离的主要目的是（ ）。
- **17（选择，指令系统 / 寻址方式）**：某计算机有 16 个通用寄存器，采用 32 位定长指令字，操作码字段（含寻址方式位）为 8 位，Store 指令的源操作数和目的操作数分别采用寄存器直接寻址和基址寻址方式。若基址寄存器可使用任一通用寄存器，且偏移量用补码表示，则 Store 指令中偏移量的取值范围是（…
- **18（选择，中央处理器 / 硬布线与微程序控制）**：某计算机采用微程序控制器，共有 32 条指令，公共的取指令微程序包含 2 条微指令，各指令对应的微程序平均由 4 条微指令组成，采用断定法（下地址字段法）确定下条微指令地址，则微指令中下地址字段的位数至少是（ ）。
- **19（选择，总线 / 总线性能与带宽）**：某同步总线采用数据线和地址线复用方式，其中地址/数据线有 32 根，总线时钟频率为 66MHz，每个时钟周期传送两次数据（上升沿和下降沿各传送一次数据），该总线的最大数据传输率（总线带宽）是（ ）。
- **20（选择，总线 / 总线事务与定时）**：一次总线事务中，主设备只需给出一个首地址，从设备就能从首地址开始的若干连续单元读出或写入多个数据。这种总线事务方式称为（ ）。
- **21（选择，输入输出系统 / DMA）**：下列有关 I/O 接口的叙述中，错误的是（ ）。
- **22（选择，输入输出系统 / DMA）**：若某设备中断请求的响应和处理时间为 100 ns，每 400 ns 发出一次中断请求，中断响应所允许的最长延迟时间为 50 ns，则在该设备持续工作过程中，CPU 用于该设备的 I/O 时间占整个 CPU 时间的百分比至少是（ ）。
- **44（综合，中央处理器 / 指令流水线）**：某程序中有如下循环代码段 P：“`for (int i=0; i<N; i++) sum += A[i];`”。假设编译时变量 sum 和 i 分别分配在寄存器 R1 和 R2 中。常量 N 在寄存器 R6 中，数组 A 的首地址在寄存器 R3 中。程序段 P 起始地址为 08048100H，对应的汇编代码和机器代码如下表所示。 | 编号 | 地址 | 机器代码 | 汇编代码 | 注释 | | :--: | :---…
- **45（综合，存储系统 / 虚拟存储、页表与TLB）**：> 【承上题·2014 年第 44 题材料】 本题与第 44 题为同一组连续大题，共用下面的程序段 P。为便于独立作答，材料一并附在此处。 某程序中有如下循环代码段 P：“`for (int i=0; i＜N; i++) sum += A[i];`”。假设编译时变量 sum 和 i 分别分配在寄存器 R1 和 R2 中。常量 N 在寄存器 R6 中，数组 A 的首地址在寄存器 R3 中。程序段 P 起始地址为 080…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
