# 2023 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 性能指标与CPU时间）**：若机器 M 的主频为 1.5GHz，在 M 上执行程序 p 的指令条数为 $5 \times 10^5$，p 的平均 CPI 为 1.2，则 p 在 M 上的指令执行速度和用户 CPU 时间分别为( )。
- **13（选择，数据表示与运算 / 整数编码与补码）**：若 short 型变量 x = -8190，则 x 的机器数为( )。
- **14（选择，数据表示与运算 / 浮点数与IEEE 754）**：已知 float 型变量用 IEEE 754 单精度浮点数格式表示。若 float 型变量 x 的机器数为 8020 0000H，则 x 的值是( )。
- **15（选择，存储系统 / 主存芯片扩展与编址）**：某计算机的 CPU 有 30 根地址线，按字节编址，CPU 和主存芯片连接时，要求主存芯片占满所有可能存储地址空间，并且 RAM 区和 ROM 区所分配的容量大小比为 3:1，若 RAM 在连续低地址区，ROM 在连续高地址区，则 ROM 的地址范围是( )。
- **16（选择，数据表示与运算 / 定点运算、标志位与溢出）**：已知 x、y 为 int 类型，当 x = 100，y = 200 时，执行 x − y 指令得到的溢出标志 OF 和借位标志 CF 分别为 0、1，那么当 x = 10，y = −20 时，执行该指令得到的 OF 和 CF 分别是( )。
- **17（选择，指令系统 / 寻址方式）**：某运算类型指令中有一个地址码为通用寄存器编号，对应通用寄存器中存放的是操作数或操作数地址，CPU 区分两者的依据是( )。
- **18（选择，中央处理器 / 数据通路与指令执行）**：数据通路由组合逻辑元件（操作元件）和时序逻辑元件（状态元件）组成。下列给出的元件中，属于操作元件的是( )。 I. 算术逻辑部件（ALU） II. 程序计数器（PC） III. 通用寄存器组（GPRs） IV. 多路选择器（MUX）
- **19（选择，中央处理器 / 指令流水线）**：在采用"取指、译码/取数、执行、访存、写回"5 段流水线的 RISC 处理器中，执行如下指令序列（第一列为指令序号），其中 s0、s1、s2、s3 和 t2 表示寄存器编号。 （含给定代码） 若采用转发（旁路）技术处理数据冒险，采用硬件阻塞方式处理控制冒险，则在 I1～…
- **20（选择，总线 / 总线性能与带宽）**：某存储器总线宽度为 64 位，总线时钟频率为 1 GHz，在总线上传输一个数据或地址需要一个时钟周期，不支持突发传送方式，若通过该总线连接 CPU 和主存，主存每次准备一个 64 位数据需要 6 ns，主存块大小为 32 B，则读取一个主存块需要的时间为( )。
- **21（选择，中央处理器 / 异常与中断）**：下列关于硬件和异常/中断关系的叙述中，错误的是( )。
- **22（选择，输入输出系统 / DMA）**：下列关于 I/O 控制方式的叙述中，错误的是（ ）。
- **43（综合，存储系统 / 虚拟存储、页表与TLB）**：已知计算机 M 字长为 32 位，按字节编址，采用请求调页策略的虚拟存储管理方式，虚拟地址为 32 位，页面大小为 4KB；数据 Cache 采用 4 路组相联映射，数据区大小为 8KB，主存块大小为 32B。现有 C 语言程序段如下： （含给定代码） 已知二维数组 a 按行优先存放，在虚拟地址空间中分配的起始地址为 0042 2000H，sizeof(int)=4，假定在 M 上执行上述程序段之前数组 a 不在主存…
- **44（综合，指令系统 / 寻址方式）**：上题中 C 程序段在计算机 M 上的部分机器级代码如下，每个机器级代码行中依次包含指令序号、虚拟地址、机器指令和汇编指令。 （含给定代码） 请回答下列问题。 (1) 第 20 条指令的虚拟地址是多少？ (2) 已知第 2 条 jmp 和第 7 条 jge 都是跳转指令，其操作码分别是 EBH 和 7DH，跳转地址分别为 0040 1084H、0040 10BCH，这两条指令都采用什么寻址方式？给出第 2 条指令 jm…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
