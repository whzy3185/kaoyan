# 2012 年 408 — 计算机组成原理摘取

## 本年计算机组成原理范围

- **单项选择题：第 12～22 题**
- **综合应用题：第 43、44 题**

## 题目定位

- **12（选择，计算机系统概述 / 性能指标与CPU时间）**：假定基准程序 A 在某计算机上的运行时间为 100 秒，其中 90 秒为 CPU 时间，其余为 I/O 时间。若 CPU 速度提高 50%，I/O 速度不变，则运行基准程序 A 所耗费的时间是（ ）。
- **13（选择，数据表示与运算 / 类型转换与符号扩展）**：假定编译器规定 int 和 short 型长度分别为 32 位和 16 位，执行下列 C 语言语句： （含给定代码） 得到 y 的机器数为（ ）。
- **14（选择，数据表示与运算 / 浮点数与IEEE 754）**：float 类型（即 IEEE754 单精度浮点数格式）能表示的最大正整数是（ ）。
- **15（选择，数据表示与运算 / 数据对齐）**：某计算机存储器按字节编址，采用小端方式存放数据。假定编译器规定 int 型和 short 型长度分别为 32 位和 16 位，并且数据按边界对齐存储。某 C 语言程序段如下： （含给定代码） 若 record 变量的首地址为 0xC008，则地址 0xC008 中的内容…
- **16（选择，存储系统 / 外部存储器）**：下列关于闪存（Flash Memory）的叙述中，错误的是（ ）。
- **17（选择，存储系统 / Cache）**：> 编者注：本题为**争议题**。组相联映射的组号划分在不同教材中不一致——唐朔飞教材按“主存块号 mod 组数”，蒋本珊教材另有划分规则，两次命题采用的教材不同。 假设某计算机按字节编址，Cache 有 4 个行，Cache 和主存之间交换的块大小为 1 个字（即 2…
- **18（选择，中央处理器 / 硬布线与微程序控制）**：某计算机的控制器采用微程序控制方式，微指令中的操作控制字段采用字段直接编码法，共有 33 个微命令，构成 5 个互斥类，分别包含 7、3、12、5 和 6 个微命令，则操作控制字段至少有（ ）。
- **19（选择，总线 / 总线事务与定时）**：某同步总线的时钟频率为 100MHz，宽度为 32 位，地址/数据线复用，每传输一个地址或数据占用一个时钟周期。若该总线支持突发（猝发）传输方式，则一次"主存写"总线事务传输 128 位数据所需要的时间至少是（ ）。
- **20（选择，总线 / 总线基本概念）**：关于 USB 总线特性的描述，错误的是（ ）。
- **21（选择，输入输出系统 / DMA）**：下列选项中，在 I/O 总线的数据线上传输的信息包括（ ）。 Ⅰ. I/O 接口中的命令字 Ⅱ. I/O 接口中的状态字 Ⅲ. 中断类型号
- **22（选择，中央处理器 / 异常与中断）**：响应外部中断的过程中，中断隐指令完成的操作，除保护断点外，还包括（ ）。 I. 关中断 II. 保存通用寄存器的内容 III. 形成中断服务程序入口地址并送 PC
- **43（综合，存储系统 / 虚拟存储、页表与TLB）**：假定某计算机的 CPU 主频为 80MHz，CPI 为 4，平均每条指令访存 1.5 次，主存与 Cache 之间交换的块大小为 16B，Cache 的命中率为 99%，存储器总线宽度为 32 位。请回答下列问题。 (1) 该计算机的 MIPS 数是多少？平均每秒 Cache 缺失的次数是多少？在不考虑 DMA 传送的情况下，主存带宽至少达到多少才能满足 CPU 的访存要求？ (2) 假定在 Cache 缺失的情况下…
- **44（综合，中央处理器 / 指令流水线）**：某 16 位计算机中，带符号整数用补码表示，数据 Cache 和指令 Cache 分离。下表给出了指令系统中部分指令格式，其中 Rs 和 Rd 表示寄存器，mem 表示存储单元地址，`(x)` 表示寄存器 x 或存储单元 x 的内容。 | 名称 | 指令的汇编格式 | 指令功能 | | --- | --- | --- | | 加法指令 | `ADD Rs, Rd` | (Rs) + (Rd) → Rd | | 算术/…

## 原卷使用

本文件只维护年份、题号和题干摘要/核心考点。刷题时以原始 408 真题的完整题面、选项、代码和图为准；若转写与原卷冲突，以原卷为准。

## 记录

- 首刷：
- 错题：
- 复做：
- 已掌握：
