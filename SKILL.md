# 408 数据结构专项复习 Skill

更新时间：2026-09-22

## 1. 本 Skill 的学习目标

本轮不是重新把教材从头“看一遍”，而是把已经学过但遗忘的知识重新激活，并逐步过渡到 408 真题。

数据结构的完成标准不是“看懂”，而是能够：

**识别结构 → 说明存储 → 独立执行操作 → 写出关键代码/算法 → 分析复杂度 → 做题时识别考法。**

## 2. 材料分层：禁止把所有资料当成同一种课

### L0：2027 王道完整主教材——“全覆盖基础框架 + 基础题库”

路径：`data-structure/sources/wangdao-2027/`

作用：
- 决定本轮必须覆盖的完整知识边界；
- 防止早期课程漏掉章节；
- 用“本节试题精选”做第一层低风险验证；
- 遇到概念遗漏时负责补全，而不是承担全部做题技巧。

王道 2027 共 8 章、404 页，完整覆盖绪论、线性表、栈队列数组、串、树、图、查找、排序。

### L1：早期基础课——“把概念讲透”

主入口：`data-structure/notes/*.md` 及对应早期分章 PDF。

已读内容显示它们适合：
- 顺序表/链表的存储与基本操作；
- 指针、头结点、front/rear 等基础约定；
- KMP、矩阵压缩、树等概念拆解；
- 从“逻辑结构 → 存储结构 → 基本操作 → 复杂度”建立直觉。

用户明显忘记基础时，优先回 L1，不要直接用大量难题硬顶。

### L2：早期加强课/做题课——“题型识别 + 高频陷阱 + 方法压缩”

主入口：旧 `数据结构` 分支的 `视频课程/` 与专题索引。

已读视频笔记表明其角色很明确：
- 时间复杂度：按倍增、累加、递归、嵌套循环分题型；
- 线性结构：指针画图、循环队列、表达式、KMP、矩阵；
- 非线性结构：树形推断、遍历、线索树、图、MST、最短路、拓扑、关键路径；
- 查找：ASL、BST/AVL/B/B+树、Hash、红黑树；
- 排序：中间序列识别、稳定性、复杂度、外部排序 I/O。

L2 只能在 L0/L1 已经理解后使用。不能用“背判断口诀”替代基本原理。

### L3：王道分节题库——“当天知识点验证”

使用 `sources/wangdao-2027/exercise-index.md` 定位“本节试题精选”。

规则：
- 当天学完一个知识块后先做这里；
- 重点观察是否能独立启动，而不是追求刷题数量；
- 错误立即回 L0/L1/L2 对应层处理。

### L4：正式 408 真题——“真正的独立验证”

正式真题已同步到本分支：`question-bank/408/`。`408真题` 分支继续保留为来源与历史归档。

只有知识块通过 L3 后才进入 L4。
第一周校准期默认 **不消耗正式 408 真题首刷**。

## 3. 每次学习前必须先做材料合并

开始一个知识块前，助手先完成内部准备：

1. 用王道 OUTLINE 确定完整范围；
2. 读取对应早期基础课笔记；
3. 读取对应加强课/视频做题法；
4. 查看王道题库的本节入口；
5. 需要正式真题时再读取本分支 `question-bank/408/按知识点/` 索引。

不能只拿一本材料单线讲到底。

## 4. 自适应路由

### A. 基本忘光
表现：术语都说不清、题目不知道在问什么。

路线：
**王道框架 → 早期基础课详细讲解 → 最小操作例子 → 王道基础题**

### B. 概念有印象但不会做
路线：
**快速扫王道 → 基础课补断点 → 加强课题型方法 → 王道题库**

### C. 基本会做但速度慢/易错
路线：
**闭卷回忆 → 加强课判断链 → 王道题库 → 后续正式 408**

### D. 某类错误反复出现
只回溯对应断点，不重学整章。

## 5. 数据结构固定分析模板

每遇到一个结构必须回答：

1. 逻辑关系是什么？
2. 物理上怎么存？
3. 关键指针/下标分别指谁？
4. 插入、删除、查找、遍历如何执行？
5. 时间复杂度是多少？
6. 空间复杂度是多少？
7. 真题看到什么信号应该想到它？

算法题再追加：
- 输入/输出；
- 核心不变量；
- 边界条件；
- 伪代码/C 风格代码；
- 时间复杂度；
- 空间复杂度。

## 6. 章节级强制能力

### 绪论
- 从执行次数推复杂度，而不是背答案；
- 倍增、累加、递归、嵌套循环能自己推。

### 线性表
- 画出顺序表/链表的真实存储；
- 区分头指针、头结点、首元结点；
- 链表改指针前先保存原后继。

### 栈/队列/数组
- 循环队列先声明 front/rear 约定；
- 表达式题明确扫描顺序和栈状态；
- 矩阵地址题先确认行/列优先、下标起点、元素字节数。

### 串
- KMP 先确认 next 定义；
- 能从最长相等前后缀理解 next，而非纯背表。

### 树
- 性质题能画极端结构；
- 遍历/重建必须自己走递归；
- 线索树区分真实孩子与线索；
- 哈夫曼反复取当前最小两棵树。

### 图
- 先定存储，再分析算法；
- BFS/DFS、MST、最短路、拓扑、关键路径明确适用条件；
- 图算法必须保留中间状态。

### 查找
- 折半 ASL 用判定树；
- BST/AVL/B/B+树画插入删除过程；
- Hash 成功/失败 ASL 分开。

### 排序
- 能手推一趟；
- 从中间序列识别算法；
- 区分稳定性、比较次数、移动次数、最好/最坏复杂度；
- 外部排序最终目标是降低 I/O。

## 7. 第一周校准规则

第一周只使用：
**L0 王道框架 + L1 基础课 + L2 加强课 + L3 王道题库。**

暂不把正式 408 真题标成“首刷”。

本周数据结构安排：
- D1：基本概念 + 时间/空间复杂度；
- D3：线性表定义 + 顺序表；
- D5：单链表/双链表/循环链表；
- D7：闭卷复测与错因归类。

具体见 `WEEK1_PLAN.md`。

## 8. 进度判定

一个知识块只有同时满足以下条件才记“本轮复习完成”：
- 不看资料能讲清核心概念；
- 最小例子可以独立完成；
- 王道分节题能独立启动；
- 错误已经解释到机制层，而非只记答案。

“教材读完”“视频看过”“以前学过”均不能直接算完成。

## 9. 记录

真实学习后更新：
- `data-structure/CURRENT_STATE.md`
- `data-structure/PROGRESS.md`

后续正式刷题时，再增加错题和延迟复测记录。

两套保留卷年份确定后必须全程黑名单化。
