# 408 计算机组成原理专项复习 Skill

更新时间：2026-09-22

## 1. 目标

本轮计组复习的目标不是重新背完所有定义，而是恢复一条贯通的机器执行链：

**数据如何表示 → 如何运算 → 如何存储 → 指令如何定位数据 → CPU 如何执行 → 流水线如何并行 → I/O 如何交换数据。**

完成标准是能够独立完成位级、地址级、时序级和数据通路级推理。

## 2. 材料分层

### L0：2027 王道——完整基础框架 + 分节题库

路径：`sources/wangdao-2027/`

作用：
- 给出 7 章完整考纲边界；
- 用章节小结/易混点查漏；
- 用“本节习题精选”作为第一层验证；
- 不替代早期课程中更细的图、例题和板书推导。

### L1：早期基础课——概念、结构、位级过程

入口：
- `pdf-ocr/*.md`
- `sources/INDEX.md` 中对应的早期分章 PDF

适合：
- 补码/定点数/IEEE754；
- 存储芯片、多模块存储器；
- Cache、虚拟存储；
- 指令格式/寻址；
- CPU、数据通路、流水线、I/O 的原始讲解。

### L2：早期加强课/配套 PPT/专题材料——题型和推导强化

仓库当前没有给每一份早期资料统一写“基础课/加强课”标签，因此 **禁止仅凭文件名猜层级**。

判定规则：
- 如果材料按定义、结构、基本机制顺序讲解 → 按 L1 使用；
- 如果材料集中做计算、真题、典型陷阱、板书推导、综合图 → 按 L2 使用；
- 同一知识点若既有 PDF 又有 PPT，学习前都要检查，按内容分工而非二选一。

### L3：王道分节题库

路径：`sources/wangdao-2027/exercise-index.md`

作用：
- 每个知识块学完后的低风险验证；
- 观察“会不会启动”“过程是否完整”；
- 不把做对一道题等同于知识点掌握。

### L4：正式 408 真题

路径：`question-bank/408/`

只在知识块通过 L3 后启用。
第一周校准期不计正式 408 首刷。

## 3. 每个知识块开始前的内部准备

助手必须先：
1. 用王道 OUTLINE 确定该知识块完整范围；
2. 读取相关早期 OCR/基础材料；
3. 检查同章节 PPT/加强材料的题型、图和易错点；
4. 定位王道本节题库；
5. 正式刷题阶段再打开 408 按知识点索引。

不能只读王道目录后直接讲，也不能只沿早期讲义忽略新版完整范围。

## 4. 自适应路由

### 基础断裂
**王道框架 → L1 早期基础课 → 数字化最小例子 → 王道基础题**

### 概念知道但不会算
**快速王道回忆 → L1 补公式来源 → L2 看典型推导 → 王道题库**

### 会算但容易混
**闭卷定义口径 → L2 易错对比 → 王道题库 → 后续 408**

### 同类错误反复出现
只回溯导致错误的前置层，不重学整章。

## 5. 计组强制推理规范

### 5.1 数制与数据表示
每题先写：
- 位宽；
- 有/无符号口径；
- 编码方式；
- 可表示范围。

类型转换、扩展、截断必须画出实际位模式。

### 5.2 定点运算
必须区分：
- 数学结果；
- 机器结果；
- 溢出；
- CF/OF/SF/ZF 等标志位口径。

不能只写最终十进制结果。

### 5.3 IEEE754
固定过程：
**十进制/二进制转换 → 规格化 → 阶码加偏置 → 尾数 → 舍入 → 特殊值/溢出检查。**

### 5.4 存储系统
地址题先写总位数，再划分字段。
Cache/TLB/页表必须画访问路径。
芯片扩展题写清字扩展、位扩展、字位扩展和片选。

### 5.5 指令系统
持续区分：
- A：形式地址；
- EA：有效地址；
- 操作数：通常为 M[EA]。

访存次数必须声明是否包含取指。

### 5.6 CPU/数据通路
必须写：
- 当前指令阶段；
- 哪个寄存器读/写；
- 总线/ALU 做什么；
- 控制信号或微操作如何产生。

### 5.7 流水线
先画时序表，再计算。
必须区分结构、数据、控制冒险以及处理方式。

### 5.8 总线/I/O
DMA、中断、程序查询按：
**谁发起 → 谁控制 → 数据走哪里 → CPU 参与多少 → 何时响应**
统一比较。

## 6. 章节能力门槛

- 第1章：能从时钟周期、CPI、指令数推 CPU 时间并比较性能。
- 第2章：能完成补码、移位、溢出、IEEE754 位级推理。
- 第3章：能独立完成 Cache/页表/TLB/芯片扩展地址题。
- 第4章：能算 EA、访存次数并读懂基本机器级代码。
- 第5章：能读数据通路、写微操作、画流水线。
- 第6章：能算带宽/事务并区分同步异步定时。
- 第7章：能比较中断/DMA/查询并完成传送过程题。

## 7. 第一周校准

只使用 L0-L3，不消耗正式 408 首刷。

本周：
- D1：计算机系统层次 + 性能指标；
- D4：数制 + 整数编码 + 类型转换；
- D6：定点加减、移位、溢出与标志位；
- D7：闭卷复测。

具体见 `WEEK1_PLAN.md`。

## 8. 进度判定

一个知识块只有满足：
- 核心定义闭卷可说；
- 至少一个数字例子独立完成；
- 王道分节题能独立启动；
- 错误原因能定位到具体口径/步骤；

才记本轮“复习完成”。

历史上学过不等于本轮通过。

## 9. 状态与保留卷

真实学习后更新：
- `CURRENT_STATE.md`
- `PROGRESS.md`

两套保留卷确定年份后全程黑名单。
