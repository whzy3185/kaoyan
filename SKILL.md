# 408 计算机网络专项复习 Skill

更新时间：2026-09-22

## 1. 目标

本轮计网不是背协议名，而是建立完整通信链：

**应用产生数据 → TCP/UDP → IP → MAC/链路 → 中间设备逐跳转发 → 对端协议栈。**

每个知识点都要能回答“为什么这样设计、数据实际怎么走、题目给什么条件时如何判断”。

## 2. 材料分层

### L0：2027 王道——全面基础框架 + 分节题库

路径：`computer-network/sources/wangdao-2027/`

作用：
- 六章完整覆盖；
- 查漏新版知识范围；
- 提供每节习题精选；
- 作为完整基础框架，不承担所有题型技巧。

### L1：早期基础课——机制与协议过程

主入口：`computer-network/pdf-ocr/*.md` 及原始分章 PDF。

已读内容显示它们适合：
- 网络概述与分层；
- 物理层通信基础；
- 数据链路层可靠传输；
- CSMA、以太网、PPP、VLAN；
- 后续原始分章 PDF 中的 IP/TCP/应用层完整机制。

L1 用来把“协议为什么存在、字段如何作用、过程怎样发生”讲清。

### L2：早期加强课/视频做题课——408 判断链和板书推导

主入口：`notes/video/*.md`

这些视频笔记已经明确呈现大量“高频真题锚点”和板书方法：
- 物理层：编码/调制、奈奎斯特、香农；
- 链路层：CDMA、CRC、CSMA/CD/CA、GBN/SR、信道利用率、VLAN；
- 网络层：IPv4、分片、CIDR、最长前缀、ARP/ICMP/NAT、RIP/OSPF/BGP；
- 传输层：TCP seq/ack、窗口、握手/挥手、拥塞控制；
- 应用层：DNS、FTP、邮件、HTTP 时延。

L2 用于把 L0/L1 的理解压缩成做题判断链，不能先背口诀再补原理。

### L3：王道分节题库

路径：`computer-network/sources/wangdao-2027/exercise-index.md`

当天知识点复习完成后先用 L3 验证。
第一周不把它算正式 408 首刷。

### L4：正式 408 真题

路径：`question-bank/408/`

共 135 条学科记录。
只有通过 L3 后才进入 L4。
两套保留卷年份确定后必须从 BY_TOPIC 中排除。

## 3. 每个知识块开始前必须融合材料

1. 王道 OUTLINE 明确完整范围；
2. 早期基础课恢复机制；
3. 视频/加强课提取高频判断链；
4. 王道题库做第一次验证；
5. 后续再进 408 真题。

不允许：
- 只看视频技巧；
- 只读王道而忽略早期做题课；
- 只背端口和字段不理解通信过程。

## 4. 自适应路由

### 基本忘记
王道地图 → L1 协议过程 → 画最小通信图 → 王道基础题。

### 概念知道但不会计算
快速王道 → L1 补公式/机制 → L2 板书推导 → 王道题库。

### 基本会但易错
闭卷过程 → L2 高频判断链 → 王道题库 → 后续 408。

### 综合题卡住
沿协议栈逐层追踪，不直接找“对应公式”。

## 5. 统一通信分析模板

每遇到一个协议/设备必须问：

1. 工作在哪一层？
2. 输入是什么？
3. 输出是什么？
4. 解决什么问题？
5. 本端会修改哪些字段？
6. 中间设备会修改哪些字段？
7. 对端如何确认/恢复？
8. 题目给什么信号时应该想到它？

## 6. 各章强制做题法

### 第1章 体系结构
时延题拆：
**发送 + 传播 + 处理 + 排队**。
分层题必须区分服务、协议、接口和 PDU。

### 第2章 物理层
- 奈奎斯特：先确认理想/无噪声与码元状态数；
- 香农：先确认带宽和信噪比；
- 编码/调制题先判断是基带还是载波问题；
- 单位统一到 Hz、bit/s、s。

### 第3章 数据链路层
- CRC 写模2除法；
- S-W/GBN/SR 画时序；
- 利用率题明确帧发送时延、传播时延、RTT、ACK；
- CSMA/CD 算碰撞检测和最小帧；
- 交换机固定“学源 → 查目的 → 转发/过滤/泛洪”。

### 第4章 网络层
IP/CIDR 固定：
**地址转二进制 → 前缀 → 网络范围 → 可用主机 → 路由匹配**。

逐跳转发固定：
**判断同网 → 查路由 → 得下一跳 → ARP → 改链路层地址**。

### 第5章 传输层
TCP 固定：
- seq = 本段第一个数据字节号；
- ack = 期望下一个字节；
- 发送窗口受 rwnd/cwnd 共同限制；
- 拥塞控制画 cwnd/ssthresh 演化；
- 握手/挥手写控制位与序号。

### 第6章 应用层
DNS/FTP/HTTP 等必须放进一次真实访问流程。
网页时延题先列 DNS、TCP 建连、对象获取阶段。

## 7. 第一周校准

第一周只用 L0-L3。

本周：
- D2：网络概述、交换、性能指标、体系结构；
- D4：码元/速率/奈奎斯特/香农；
- D5：编码、调制、传输介质、Hub/中继器；
- D7：闭卷复测。

具体见 `WEEK1_PLAN.md`。

## 8. 进度判定

一个知识块只有：
- 闭卷能描述通信过程；
- 最小计算/判断能独立完成；
- 王道题库可以自己启动；
- 错因能定位到层次/字段/时序/单位/地址；

才记“本轮复习完成”。

视频“已整理”不等于用户已学会。

## 9. 状态

真实学习后更新根目录：
- `CURRENT_STATE.md`
- `PROGRESS.md`

两套保留卷确定后在所有专项抽题入口黑名单化。
