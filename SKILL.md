# 408 操作系统专项复习 Skill

更新时间：2026-09-22

## 1. 目标

操作系统不能靠背定义完成。本轮要求把知识转换为可推演的：

**状态图、时间轴、资源关系、地址转换链、文件访问链和 I/O 流程。**

## 2. 材料分层

### L0：2027 王道——完整基础框架 + 分节题库

路径：`sources/wangdao-2027/`

作用：
- 完整覆盖 5 章；
- 补齐早期分拆资料原本缺失的 3.1 内存管理概念；
- 给出每节“习题精选”和章末疑难点；
- 作为本轮知识边界和查漏基线。

### L1：早期基础课——机制讲解和过程理解

来源见 `sources/INDEX.md`：
- 1-1～1-6；
- 2-1～2-6；
- 3-2、3-3；
- 4-1、4-2；
- 5-1、5-2。

这些分拆材料用于把具体机制讲透，例如：
- 进程状态与线程；
- 调度；
- 同步互斥；
- 内存分配/虚存；
- 文件和 I/O。

### L2：早期加强课/专题内容——综合题建模和高频陷阱

用户确认早期资料中同时存在基础课和加强课。当前 GitHub 索引没有给每个早期 PDF 统一写课程标签，因此：

**不能凭文件名擅自认定。**

使用原文件时按内容识别：
- 顺序讲概念/机制 → L1；
- 大量真题、综合题、专题算法、易错比较、复杂状态推演 → L2。

L2 的作用是：
- 压缩成解题判断链；
- 训练调度/PV/死锁/页表/文件索引/磁盘调度等综合过程；
- 不能替代 L1 的机制理解。

### L3：王道分节题库

`sources/wangdao-2027/exercise-index.md` 已定位各节习题和答案。

每个知识块复习后先做 L3。
第一周用它测真实基础，不计正式 408 首刷。

### L4：正式 408 真题

`question-bank/` 中 2009—2023 共 184 条学科记录。

通过 L3 后才进入 L4。
保留卷年份确定后必须跳过。

## 3. 每个知识块的准备顺序

1. 王道 OUTLINE 确定范围和新版补充内容；
2. 读取对应早期分拆 PDF 的基础解释；
3. 检查同主题加强课/专题题；
4. 用王道习题完成第一次验证；
5. 后续才调用 408 按知识点题库。

早期材料和王道不是互相替代，而是：
**王道保完整，早期课保理解和题感。**

## 4. 自适应路由

### 完全忘记
王道建立地图 → 基础课逐机制讲 → 画最小状态/流程 → 王道基础题。

### 概念记得但过程不会
快速王道 → 早期课程完整推一例 → 加强课方法 → 王道题库。

### 会做但容易混
直接比较易混概念 → 加强课综合题 → 王道题库 → 后续 408。

### 同类错误重复
回到错误发生的上一层：
- 状态错误 → 重画状态图；
- 顺序错误 → 重画时间轴；
- PV 错误 → 重建制约关系；
- 地址错误 → 重拆地址；
- 文件错误 → 重画索引/块结构。

## 5. 操作系统统一做题模板

### 进程/线程
固定问：
- 谁拥有资源？
- 谁是调度单位？
- 当前状态是什么？
- 哪个事件触发状态变化？
- 是否发生上下文切换？

### CPU 调度
固定步骤：
1. 列到达时间/服务时间/优先级；
2. 确认抢占性；
3. 画时间轴；
4. 算完成时间；
5. 再算周转/带权周转/等待等指标。

禁止直接套平均值公式。

### 同步与互斥 / PV
固定七步：
1. 找并发实体；
2. 找临界资源；
3. 画前驱/制约关系；
4. 区分同步与互斥；
5. 定义每个信号量的语义；
6. 定初值；
7. 放 P/V 并检查死锁、并发度和重复唤醒。

### 死锁
必须区分：
- 死锁；
- 饥饿；
- 不安全状态。

银行家算法保留：
**Allocation / Max / Need / Available / Work / Finish**。

### 内存
固定链：
**逻辑地址 → 页/段号与偏移 → 页表/TLB → 页框 → 物理地址 → 缺页与置换。**

页面置换每一步写驻留集合。

### 文件
先分：
- 逻辑结构；
- 目录；
- 物理分配；
- 索引节点；
- 空闲空间。

索引文件题必须逐层计算地址项和可寻址数据块。

### I/O
先判断：
- 程序查询 / 中断 / DMA；
- CPU 何时参与；
- 数据经哪里传；
- 哪一步可能阻塞。

磁盘调度必须画磁头移动序列。

## 6. 章节能力门槛

- 第1章：能区分系统调用/过程调用、用户态/内核态、中断/异常。
- 第2章：能画进程状态、调度时间轴、PV 关系、银行家算法。
- 第3章：能完整完成分页/TLB/虚存/置换过程。
- 第4章：能计算索引文件、目录与空闲空间管理。
- 第5章：能推中断/DMA/缓冲/磁盘调度。

## 7. 第一周校准

第一周使用 L0-L3，不计正式 408 首刷。

本周：
- D2：OS 基本概念 + 运行环境；
- D3：进程/线程 + 状态转换；
- D6：CPU 调度；
- D7：闭卷复测。

具体见 `WEEK1_PLAN.md`。

## 8. 第一周为什么暂不刷正式 408

这一周的目的不是增加题量，而是测出：
- 一次知识块复习实际需要多久；
- 你遗忘到哪一层；
- 早期基础课和加强课分别需要调用多少；
- 王道题库通过率如何。

D7 后再决定第二周真题同步量。

## 9. 进度判定

只有：
- 核心机制能闭卷解释；
- 最小过程能独立画出；
- 王道题能独立启动；
- 错因能归到具体状态/顺序/资源/地址层；

才记“本轮复习完成”。

真实学习后更新 `CURRENT_STATE.md`、`PROGRESS.md`。
