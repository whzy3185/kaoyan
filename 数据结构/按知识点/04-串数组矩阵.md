# 04 串、数组与矩阵

> 包含 KMP、数组地址计算、特殊矩阵和稀疏矩阵。

## 串 / KMP

- **2015-08（选择）**：KMP 匹配第一次失配后，依据 next 信息判断下一次比较时 i、j 的值。
- **2019-09（选择）**：给定主串和模式串，KMP 匹配直到成功时的字符比较次数。

## 数组地址与压缩存储

- **2016-04（选择）**：100 阶三对角矩阵按行优先压缩到一维数组，计算指定元素下标。
- **2018-03（选择）**：12×12 对称矩阵上三角按行优先压缩存储，计算指定元素下标。
- **2020-01（选择）**：10×10 对称矩阵上三角按行优先压缩存储，计算指定元素下标。
- **2021-03（选择）**：二维数组按行优先存储，根据两个元素地址反推另一个元素地址。

## 稀疏矩阵

- **2017-03（选择）**：判断适用于压缩存储稀疏矩阵的两种存储结构。
- **2023-03（选择）**：采用三元组表存储稀疏矩阵时，除三元组外还需要保存哪些矩阵信息。
