# 06 图

> 包含图的基本性质、存储、遍历、拓扑排序、最短路径、最小生成树与关键路径。

## 图的基本性质与存储

- **2009-07（选择）**：判断无向连通图的度数、边数等性质。
- **2011-08（选择）**：判断回路、稀疏图存储以及拓扑序列相关叙述。
- **2013-07（选择）**：给定邻接矩阵，计算各顶点的度。
- **2017-07（选择）**：无向图给定边数及若干顶点度数，求可能的最少顶点数。
- **2022-06（选择）**：根据 |V| 与 |E| 的关系判断图是否必然连通或不连通。

## DFS / BFS

- **2012-05（选择）**：邻接表存储有向图时，BFS 的时间复杂度。
- **2013-08（选择）**：给定无向图，判断哪个序列不可能是 BFS 序列。
- **2015-05（选择）**：从指定顶点开始 DFS，统计可能得到的不同遍历序列数量。
- **2016-06（选择）**：给定有向图，判断哪个序列不是 DFS 序列。
- **2020-06（选择）**：修改递归 DFS 的访问输出位置后，分析所得访问序列与原图/逆图遍历的关系。

## 拓扑排序 / DAG

- **2010-07（选择）**：无向图含 7 个顶点，求在任何情况下都保证连通所需的最少边数。
- **2010-08（选择）**：给定有向无环图，统计可能得到的不同拓扑序列数量。
- **2012-06（选择）**：依据有向图邻接矩阵的零元素分布，判断拓扑序列是否存在及是否唯一。
- **2014-07（选择）**：给定有向图，判断可能的拓扑序列。
- **2016-07（选择）**：邻接表存储有向图时，拓扑排序算法的时间复杂度。
- **2018-07（选择）**：给定有向图，判断哪个序列不是拓扑序列。
- **2021-07（选择）**：给定有向图，统计拓扑有序序列数量。
- **2019-06（选择）**：用有向无环图表示指定表达式，求至少需要的顶点数。

## 最短路径

- **2012-07（选择）**：Dijkstra 算法从指定源点出发，判断各最短路径目标顶点被确定的先后次序。
- **2016-08（选择）**：Dijkstra 算法从顶点 1 出发，判断各最短路径目标顶点被确定的次序。
- **2021-08（选择）**：Dijkstra 算法求源点到其余顶点最短路，第二条最短路径确定后判断 dist 数组内容。
- **2023-06（选择）**：无向连通图边权均为 1，判断哪些算法一定能够求单源最短路径。
- **2009-41（综合）**：给出一种“每次选离当前顶点最近的边”的最短路径方法，判断其是否总能求得最短路径并说明理由。

## 最小生成树 MST

- **2012-08（选择）**：判断关于最小生成树、Prim 和 Kruskal 的叙述。
- **2015-06（选择）**：同一带权图中，判断 Kruskal 与 Prim 在第 2 次选择的边可能出现的差异。
- **2020-07（选择）**：给定带权图，用 Kruskal 算法求 MST，判断应加入的边序列。
- **2017-42（综合）**：使用 Prim 求 MST，并讨论最小生成树是否唯一及唯一条件。
- **2018-42（综合）**：城市光纤网络最经济铺设方案，本质为 MST；同时考查图的存储结构和算法选择。

## AOE 网 / 关键路径

- **2013-09（选择）**：AOE 网中若同时加快若干活动，判断哪些组合能够缩短总工期。
- **2019-05（选择）**：AOE 网中指定活动的最早开始时间和最迟开始时间。
- **2020-08（选择）**：判断关于 AOE 网、关键路径和工程工期的叙述。
- **2022-07（选择）**：给定 AOE 网，判断余量最大的活动。
- **2011-41（综合）**：有向带权图的邻接矩阵按行优先压缩到一维数组，恢复矩阵与图，并求关键路径及长度。

## 邻接矩阵与路径计数

- **2015-42（综合）**：写出图的邻接矩阵，解释 A² 中指定元素的含义，并推广到 A^m 中非零元素的路径含义。

## 欧拉路径

- **2021-41（综合）**：邻接矩阵存储的无向连通图，判断是否存在包含全部边且长度为 |E| 的欧拉路径。

## 顶点入度 / 出度算法

- **2023-41（综合）**：邻接矩阵存储有向图，输出所有出度大于入度的 K 顶点并返回 K 顶点个数。
